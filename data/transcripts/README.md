# Transcript cache

Transcript JSON files are generated by `scripts/download_transcripts.py` and are intentionally not ignored by default: timestamped transcript evidence is a derived research artifact that can be versioned when licensing and repository size permit.

Raw audio and video remain ignored. Each file records the ScrapeCreators response, including the video ID, language, transcript segments, and timestamp fields.

